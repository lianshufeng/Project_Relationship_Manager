# 项目开发规范

## 规范维护

- 本文件是项目开发与数据建模规范的唯一说明入口。
- 任何架构约定、业务模型、数据字段、目录结构、接口行为或开发流程发生变化时，必须在同一次修改中同步更新本文件。
- 不允许只修改代码而遗漏对应规范；提交前必须检查本文件是否需要更新。

## 语言与编码

- 沟通、界面文案和项目说明统一使用中文。
- 所有新建或修改文件必须采用 UTF-8 无 BOM 编码。
- 只做满足需求的最小修改，禁止无关重构和整文件重写。
- 修改完成后必须回读相关文件，检查中文无乱码、无 Unicode 替换字符。

## 数据存储与接口

- 项目业务数据只有一个数据源：`store/project-relationship-data.json`。
- 前端只能通过 `/api/project-data` 读取和保存项目数据，不得直接读取 `public` 下的业务 JSON。
- `GET /api/project-data` 读取 `store/project-relationship-data.json`；`PUT /api/project-data` 原子写入同一文件。
- 当 `store/project-relationship-data.json` 不存在时，服务端必须自动创建可运行的空数据：仅保留一个“未命名项目部”根节点，其余实体与关系为空，不得让前端因缺少文件而报错。
- `store/project-relationship-data.json` 必须纳入版本管理，保证首次启动即可读取数据。
- `public` 目录不得保存项目默认数据或业务数据副本，避免出现读写不一致。
- 数据结构变化时，必须同步修改 `src/types.ts`、`src/data.ts`、`src/App.tsx` 和 `store/project-relationship-data.json` 中的数据版本，并提供旧字段清理逻辑。

## 编辑与保存

- 页面默认处于可编辑状态，不设置额外的“进入编辑模式”。
- 实体修改、关系维护、拖拽排序和布局调整先保存在浏览器内存中。
- 只有用户点击顶部“保存”或使用保存快捷键时，才通过接口写入服务器。
- “重新加载已保存数据”从 `store` 重新读取，并明确提示未保存修改会丢失。

## 业务模型

- 项目部是系统根节点，必须提供独立的“项目信息”维护入口，支持维护项目名称、项目简称、项目地址和项目介绍。
- 岗位必须归属项目部，人员必须归属至少一个岗位。
- 人员不直接关联区域；人员可以通过关系领用或使用设备。
- 固定设备可以通过“安装于”关系归属区域；佩戴设备、机械绑定设备和移动设备不归属区域。
- 设备实例继承设备类型的图标，不单独保存图标。
- 设备与人员、岗位、系统的业务关联统一使用关系数据表达，不在设备对象中重复保存绑定字段。
- 系统的上级系统使用“包含”关系表达，不在系统对象中重复保存上级字段。
- 同一业务含义只保留一个数据来源；编辑表单中的关系字段必须从现有关系回填，并在保存时同步维护关系。

## 验证要求

- 每次修改后至少运行 `npm run build`。
- 涉及业务数据时必须验证 JSON 可解析、数据版本一致、关键实体与关系存在。
- 涉及字段删除时必须同时检查类型、表单、详情展示、默认数据和兼容清理逻辑。
- 验证修改文件均为 UTF-8 无 BOM，且中文未乱码。
