# 项目开发规范

## 规范维护

- 本文件是项目开发与数据建模规范的唯一说明入口。
- 任何架构约定、业务模型、数据字段、目录结构、接口行为或开发流程发生变化时，必须在同一次修改中同步更新本文件。
- 不允许只修改代码而遗漏对应规范；提交前必须检查本文件是否需要更新。

## 语言与编码

- 沟通、界面文案和项目说明统一使用中文。
- 所有新建或修改文件必须采用 UTF-8 无 BOM 编码。
- 只做满足需求的最小修改，禁止无关重构和整文件重写。
- 修改完成后必须回读相关文件，检查中文无乱码、无 Unicode 替换字符。

## 数据存储与接口

- 项目运行时业务数据只有一个数据源：MongoDB 的 `entities`、`relations`、`graph_layouts`、`graph_settings`、`entity_feedbacks`、`feedback_attachments`、`person_activity_records` 以及 GridFS bucket `feedback_images.files`、`feedback_images.chunks`。
- `store/project-relationship-data.json` 仅作为首次迁移源和历史备份，不得参与运行时读写；JSON 只允许用于导入导出和迁移，禁止与 MongoDB 双写。
- 服务端统一使用 NestJS + Mongoose，前端只能通过 `/api/v1` 访问业务数据，不得直接连接 MongoDB。
- MongoDB 没有项目数据时属于正常空状态，服务端不得自动写入默认项目；前端必须提供创建空白项目和导入 JSON 的入口，不得显示数据载入错误。
- 项目配置必须支持 JSON 导入和导出；导入必须经过版本、结构、引用和事务校验，并允许不超过 300 MB 的 JSON 请求，导出必须读取 MongoDB 最新数据并包含反馈记录、图片与人员活跃度历史；反馈和人员活跃度属于当前配置的必要结构，不兼容缺少对应字段的当前版 JSON。
- 完整图谱只允许通过 `GET /api/v1/projects/:projectId/graph` 聚合读取，并且只返回画布、搜索、关系计算和反馈统计所需的轻量字段；实体说明、电话、地址、编码等详情必须在选中或编辑实体时通过 `GET /api/v1/projects/:projectId/entities/:entityId` 按需读取。普通编辑禁止整体覆盖图谱，必须使用实体、关系和布局的细粒度 CRUD 接口。
- MongoDB 连接串只允许通过 `MONGODB_URI` 环境变量提供，真实账号、密码和地址不得写入版本库、日志或 `.env.example`。
- 核心 Mongoose Schema 必须使用 `strict: 'throw'`，不得使用无约束的 `Mixed` 保存业务字段；嵌套或派生数据必须有明确类型。
- 反馈图片二进制必须存入 GridFS，`feedback_attachments` 只保存严格类型的业务元数据和 GridFS 文件 ID；GridFS 不支持多文档事务，必须采用先上传、元数据事务提交、失败补偿删除和服务启动时幂等清理孤立文件的策略。
- 高频查询必须由匹配筛选与排序顺序的复合索引覆盖；反馈主记录分页、处理记录顺序、附件归属、人员活跃度今日聚合与历史查询、GridFS chunk 顺序和 GridFS 业务元数据均必须维护对应复合索引，禁止仅依赖多个低选择性的单字段索引。
- 实体 ID 和关系 ID 使用稳定字符串；`schemaVersion` 表示结构版本，业务记录的 `revision` 表示并发版本，两者不得混用。
- 具体设备和系统必须保存 `0` 到 `100` 且以 `10` 为单位的整数人工使用状态评估值 `activityLevel`，只允许 `0%`、`10%`、`20%` 至 `100%` 十一个档位，默认值为 `0`；其他实体禁止保存该字段。评估值最后调整时间 `activityUpdatedAt` 必须由服务端在数值变化时生成；界面统一使用“使用状态评估”，不得使用“活跃度”，该值表示人工评估的当前使用程度，不表示故障或健康状态。
- 人员活跃度与设备、系统的使用状态评估完全独立。人员活跃度按项目、人员、项目时区日期、来源类型和来源 ID 保存于 `person_activity_records`；来源类型只允许设备类型 `deviceType` 或具体系统 `product`，不得引用设备实例。`activityValue` 必须是大于等于 `0` 的安全整数，无记录等同于 `0`，设置为 `0` 时删除对应稀疏记录；同一人员、日期和来源只允许一条记录。
- 数据结构变化时必须同步修改 Mongoose Schema、DTO、服务端业务校验、前端类型和幂等迁移脚本；已执行迁移禁止修改，只能新增迁移。
- 多集合一致性操作必须在 MongoDB Replica Set 事务中完成，包括带关系的实体创建、实体删除和配置导入。
- MongoDB 不提供外键，服务端保存前必须校验实体 ID 唯一、关系端点存在、引用属于同一项目以及所有业务关系约束。

## 编辑与保存

- 页面默认处于可编辑状态，不设置额外的“进入编辑模式”。
- 实体和关系的新增、修改、删除在用户确认后立即调用对应 CRUD 接口并同步 MongoDB，不得等待整图保存。
- 关系列表拖拽后必须按项目全量关系顺序持久化，并同步重新计算当前关系图布局；同层关联节点的渲染顺序必须与关系列表一致。
- 关系列表中的对象名称允许省略显示，但鼠标悬浮关系卡片任意位置时必须展示完整关系名称和对象名称。
- 关系图节点中的名称和摘要允许省略显示，但鼠标悬浮节点整卡时只展示完整名称、摘要、关联数量和反馈概要；具体设备与系统的使用状态评估只允许在右侧灯柱的独立悬浮提示中展示，不得在整卡提示中重复显示。
- 具体设备和系统的实体详情必须直接提供 `0%` 到 `100%` 的使用状态评估档位点选和实时预览，评估值不得放入新增或编辑窗口；`10%` 到 `100%` 使用 10 格分段灯带直接点选，`0%` 提供独立紧凑入口，并显示一句简短的自动保存操作提示。点选后必须经过尾随防抖自动调用实体 CRUD 接口静默保存，连续或并发调整必须保证数据库最终写入最后一次点选值，详情卡片不得显示保存状态或最后调整时间，保存失败只通过全局错误提示反馈。关系图节点和实体详情必须显示独立的使用状态指示灯，不得覆盖实体类型色，其他实体不得显示该评估值。关系图节点右上侧必须使用适合完整关系图导出的短竖向灯柱，以填充高度、颜色和光晕表达评估值，不显示数字百分比；鼠标悬浮灯柱时必须显示使用状态档位、精确百分比和人工评估口径概要。精确数值在实体详情中显示。`70%` 起显示可辨识的增强轮廓与光晕；指示灯亮度必须采用非线性变化，`70%` 以后逐步增强光晕，实体详情使用 10 格分段灯带直观区分中间值与 `100%` 满亮状态。
- 人员活跃度必须直接在人员详情卡中调整，不得放入人员编辑窗口；详情卡默认选择今日，并允许切换单日或不超过 `366` 天的批量日期范围。设备类型与系统在详情卡内分区紧凑展示，默认只展示活跃值大于 `0` 的来源，并提供“编辑 / 完成”入口展开全部来源或恢复精简展示；每项直接提供非负整数输入，未保存修改存在时才显示保存操作，保存成功后立即隐藏。范围修改表示将所填值覆盖到范围内每一天，只提交发生变化的来源并通过独立批量接口在事务中保存。关系图人员节点只显示今日的设备类型和系统双轨微型热力图，不显示数字；鼠标悬浮热力图时只提示活跃值大于 `0` 的来源及其数值，完整历史不得进入完整图谱接口。
- 关系图节点双击不得打开编辑窗口；实体编辑必须通过详情面板或实体管理入口发起。
- 关系图节点右键必须禁用浏览器默认菜单，并提供编辑和删除操作；菜单必须避开视口边缘且文字不可选中，项目部根节点不得通过此入口删除。
- 节点选择和右键操作只允许更新高亮、详情与菜单，不得触发整图自动布局；右键菜单必须优先响应，避免被图谱渲染阻塞。
- 所有实体新增和编辑窗口中的图标字段必须使用可点击的图标按钮触发可视化选择面板，不得显示输入框以及组件或代码层的图标名称；图标按钮必须位于窗口标题前并显示当前图标与可编辑提示，名称字段在表单中独占一行，设备实例继续继承设备类型图标且标题不显示图标按钮。
- 每个实体详情必须在关联对象区域后提供反馈记录，关联对象和反馈记录的新增入口必须紧凑放置在各自分区标题右侧，不得在列表底部占用独立按钮行；反馈及其处理记录均支持文字和图片、服务端发布时间、编辑与删除，主反馈按时间降序分页，处理记录按时间正序展示。
- 反馈的记录人为选填项，未填写时界面统一显示“未填写”；没有处理记录时自动显示为待处理，只要存在处理记录就自动显示为已处理，删除最后一条处理记录时恢复为待处理，不得由用户手工选择状态。每条记录最多包含 20 张不超过 10 MB 的 JPG、PNG 或 WebP 图片，正文和图片至少填写一项。
- 反馈图片上传区必须支持文件选择，反馈内容框和图片上传区均须支持 `Ctrl+V` 及浏览器右键粘贴剪贴板图片；普通文字在内容框内必须保持正常粘贴，不得被图片处理逻辑拦截；不同来源的图片必须经过相同校验，并以一致的可移除缩略图形式展示在待上传区域。
- 浏览器 multipart 上传的 UTF-8 文件名必须在写入附件元数据和 GridFS 前正确解码并统一为 NFC，禁止将 Latin-1 误解码后的乱码文件名写入数据库；反馈功能不提供旧附件、旧配置或历史乱码数据的兼容迁移。
- 关系图节点仅在存在反馈时显示反馈总数；存在尚无处理记录的反馈时必须以更醒目的待跟进数量提示，统计由完整图谱接口聚合返回，不得逐节点请求。
- 反馈正文与附件必须独立存储，完整图谱不得返回反馈正文或图片；删除主反馈、实体或项目时必须在事务内级联清理处理记录和附件。
- 单节点拖拽使用实体布局接口即时同步；自动布局允许批量提交坐标，但不得携带实体或关系数据。
- 页面不提供全局保存或布局同步按钮，也不拦截 `Ctrl+S`；所有实体、关系和布局变更必须由对应操作即时写入服务端。
- 数据菜单中的清空操作只允许删除当前项目，必须使用服务端事务，并要求用户输入完整项目名称二次确认；界面必须醒目标明操作不可撤回并提供 JSON 备份入口。
- “重新加载服务器数据”重新调用完整图谱查询接口读取 MongoDB 最新数据。
- 前端撤销、重做不得只修改浏览器状态；如保留该能力，必须实现对应的补偿性服务端操作。
- 首次打开宽度不超过 `900px` 的窄屏页面时，左右面板必须默认收起，只显示中间关系画布；展开任一侧面板时必须自动收起另一侧。

## 业务模型

- 项目部是系统根节点，必须提供独立的“项目信息”维护入口，支持维护项目名称、项目简称、项目地址和项目介绍。
- 岗位必须归属项目部，人员必须归属至少一个岗位。
- 人员不直接关联区域；人员可以通过关系领用或使用设备。
- 固定设备可以通过“安装于”关系归属区域；佩戴设备、机械绑定设备和移动设备不归属区域。
- 设备实例继承设备类型的图标，不单独保存图标。
- 设备与人员、岗位、系统的业务关联统一使用关系数据表达，不在设备对象中重复保存绑定字段。
- 系统的上级系统使用“包含”关系表达，不在系统对象中重复保存上级字段。
- 同一业务含义只保留一个数据来源；编辑表单中的关系字段必须从现有关系回填，并在保存时同步维护关系。

## 验证要求

- 每次修改后至少运行 `npm run build`，服务端相关修改还必须验证健康检查和关键 API。
- 涉及业务数据时必须验证 MongoDB 可连接、实体与关系数量一致、无重复 ID、无孤立关系且关键业务约束成立。
- 涉及迁移时必须验证脚本幂等、事务失败不留下部分数据、迁移前后数量一致，并记录源文件哈希。
- 涉及 CRUD 时必须验证新增、查询、修改、删除和清理关系的完整链路；测试数据必须在验证结束后清理。
- 涉及字段删除时必须同时检查类型、表单、详情展示、默认数据和兼容清理逻辑。
- 验证修改文件均为 UTF-8 无 BOM，且中文未乱码。

## 架构与部署

- 项目采用模块化单体：Vite + React 前端、NestJS 服务端、Mongoose ODM 和 MongoDB Replica Set，不得在没有架构决策的情况下拆分微服务。
- 开发环境由 Vite 提供前端热更新并将 `/api` 代理到 NestJS；生产环境由 NestJS 通过 `@nestjs/serve-static` 同时提供 API 和 `dist/client` 静态文件。
- 开发环境前端首次载入时必须容忍 NestJS 编译启动时间，并在短时间内自动重试数据请求。
- `npm run build` 必须同时生成 `dist/client` 和 `dist/server`；`npm start` 只启动 `dist/server/main.js`，对外提供一个端口。
- 正式交付优先使用多阶段 Docker 镜像；镜像不得包含 `.env`、`store`、源码缓存或开发依赖。
- 服务必须提供 `/api/health/live` 和 `/api/health/ready`，就绪检查必须包含 MongoDB 连接状态。
- 生产进程不得依赖 Vite Preview，不得将真实连接串打包进前端或镜像层。
